# CORRECTION47 — S46 Live Side Effect Inventory

Tracked modified:
  - tovarisch/labs/memory/canary-image-build.json
      (regenerated by the S46 canary image build; restored to the
      committed S46 placeholder via `git restore` before S47 freeze.)

Untracked:
  - .factory/bin/extract-image-metadata
  - .factory/bin/tovarisch-memory-lab
  - .factory/bin/tovarisch-memory-lab-cli
  - .factory/descriptor-correction01/descriptor-correction01-tests.json
  - .factory/gate-summary.json
  - tovarisch/.factory/bin/extract-image-metadata
  - tovarisch/.factory/bin/tovarisch-memory-lab
  - tovarisch/.factory/bin/tovarisch-memory-lab-cli

Ignored (via .gitignore entries 35-40):
  - .factory/bin/extract-image-metadata
  - .factory/bin/tovarisch-memory-lab
  - .factory/bin/tovarisch-memory-lab-cli
  - tovarisch/.factory/bin/extract-image-metadata
  - tovarisch/.factory/bin/tovarisch-memory-lab
  - tovarisch/.factory/bin/tovarisch-memory-lab-cli

Outside repository (removed during cleanup):
  - .factory/  (deleted recursively)
  - tovarisch/.factory/  (deleted recursively)

Resolution actions:
  1. Removed .factory/ and tovarisch/.factory/ recursively; the
     bins were gitignored outputs of the S46 canary image build
     and the S46 controller binary build.
  2. `git restore -- tovarisch/labs/memory/canary-image-build.json`
     returned the S46 source metadata to the committed placeholder
     so the S47 source freeze is not contaminated by S46 live outputs.
